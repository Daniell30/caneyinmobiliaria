<!doctype html>
<html lang="{{ site.lang | default('es') }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  {# Import the SEO macro #}
  {% from "macros/seo.njk" import tags as seo %}

  {# Emit SEO tags (handles <title>, description, canonical, OG/Twitter, JSON-LD) #}
  {{ seo(pageTitle or title or site.name, pageDesc, pageImage) }}

  {# Your CSS links — adjust pathing if needed #}
  <link rel="stylesheet" href="/CSS/Caneyvisual.css">
</head>
<body>
  {{ content | safe }}
</body>
</html>
